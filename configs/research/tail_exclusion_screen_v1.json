{
  "schemaVersion": "tail_exclusion_screen_v1",
  "status": "research_only_shadow_only_not_trading",
  "purpose": "The horizon frontier showed the book has no day-neutral selection edge at any holding horizon from 1 to 20 sessions under either weighting, so it cannot be used to pick winners. Its own reliability gate tells a different story about the other tail: severe-loss AUC passes on validation and shadow while up-AUC fails on shadow. This module asks whether that asymmetry is a usable exclusion signal - whether the worst-ranked slice of the cross-section carries a severe-loss rate materially and stably above the eligible universe on the same day.",
  "basePrecisionConfig": "configs/research/perception_xalpha_horizon_precision_v3.json",
  "preregisteredHypothesis": {
    "factorDefinitionsDirectionsAndPriorWeightsFrozen": true,
    "bucketGridFrozenBeforeEvaluation": true,
    "horizonGridFrozenBeforeEvaluation": true,
    "parametersFrozenBeforeHistoricalEvaluation": true,
    "validationAndShadowAreRejectOnly": true,
    "historicalRunCanPromote": false,
    "countsAsNewResearchTrials": 1
  },
  "data": {
    "signalTiming": "day_t_close_past_only",
    "hypotheticalEntryTiming": "day_t_plus_1_open_if_buyable",
    "maximumExitDelayTradingDays": 5,
    "severeLossThreshold": -0.03
  },
  "screen": {
    "bucketCount": 10,
    "holdingTradingDaysGrid": [1, 5, 10, 20],
    "books": ["frozen_prior", "equal_weight"],
    "benchmark": "same_day_eligible_universe_mean"
  },
  "evaluation": {
    "excessMeasuredAgainstSameDayUniverse": true,
    "dayClusteredTRequired": true,
    "bothWindowsMustAgree": true,
    "worstBucketMinimumExcessSevereLossRate": 0.01,
    "minimumDayClusteredT": 2.0,
    "historicalWindowsAlreadyViewed": true,
    "freshForwardStillRequired": true
  },
  "output": {
    "root": "outputs/edge_research/tail_exclusion_screen_v1",
    "ordersAlwaysEmpty": true
  },
  "knownLimitations": [
    "An exclusion screen is only useful against a universe that is otherwise being selected from; it produces no return on its own and this module measures no portfolio.",
    "Excluding the worst bucket also removes whatever upside it contained, so the return excess of the excluded slice is reported alongside its severe-loss excess and must be read together.",
    "Severe loss is defined on the executable gross return at the chosen horizon and ignores borrow, limit-locked exits and any impact beyond the frozen cost assumption.",
    "Bucket boundaries are fixed deciles chosen before evaluation; a screen tuned to a different cut after seeing this run would carry selection exposure that these numbers do not price.",
    "Every historical evaluation window has already been viewed, so this run can reject a screen but can never validate or promote one.",
    "A stable historical tail ranking is not a guarantee of future tail behaviour, particularly across a regime change of the kind visible between these two windows."
  ],
  "safety": {
    "mayReadTradingConfig": false,
    "mayWriteTradingConfig": false,
    "mayWriteStrategyOverlay": false,
    "mayCallBroker": false,
    "mayCreateOrders": false,
    "mayAlterBuildDecision": false,
    "mayAlterRiskGate": false,
    "mayAlterPositionSizing": false,
    "mayPromoteAutomatically": false,
    "mayConnectToTrading": false,
    "outputStatus": "diagnostic_only"
  }
}
